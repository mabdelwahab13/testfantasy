import 'package:flutter/material.dart';

class SecondDivision extends StatelessWidget{
  const SecondDivision({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
   return Scaffold(
      appBar:AppBar(title: const Text('Second Divison'),),
      body: Container(),
    );
  }

}